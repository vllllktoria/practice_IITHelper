import React from "react"
import ScheduleForm from "../components/ScheduleForm"

function Schedule(){ 
        return( 
            <div>
                <h1>Расписание</h1>
                <ScheduleForm />
            </div>
        )
    }

export default Schedule